# 一、开启机械臂驱动

## 1.
```c++
ros2 run arm_control arm_driver_for_aubo
```
#### 若需开启键盘控制上下电
```c++
ros2 run arm_control arm_driver_for_aubo --ros-args -p use_keyboard:true

```

# 二、开启行为树规划节点(从中读取xml行为树)

### 1.
```c++
ros2 run control_master massage_planning_node
```

# 三、 开启深度相机功能包中可视化进程节点计算{target_point}
```c++
ros2 run depth_camera visual_process
```