方法1 
```c
# 初始化时选择
auto protocol_v2 = std::make_shared<ProtocolV2>(logger, clock, db_manager);
manager_->setActiveProtocol(protocol_v2);
```
方法2
```c
# ROS2参数配置
ros2 run massage_head_manager massage_head_manager --ros-args -p protocol_version:=v2
```
方法3
```c
# 运行时切换
void switchToProtocolV2() {
    auto protocol = std::make_shared<ProtocolV2>(/*...*/);
    manager_->setActiveProtocol(protocol);
}
```
