
# 一、路径代价估计函数
```cpp
F(c) = G(c) + H(c)
```

```cpp
----------------------------------------------------------------
其中：
-G(c) = 从起点 x_start 到节点 c 的**实际路径代价**

- **计算方式**：通过树结构累加得到
    
- **性质**：这是**精确已知**的数值
    
- **物理意义**：已经探索的路径部分的真实代价
```

```cpp
-H(c) = 从节点 c 到终点 x_goal 的**启发式估计代价**

----------------------------------------------------------------
- **计算方式**：**未知，需要估计**
    
- **性质**：这是**启发式估计**的数值
    
- **物理意义**：尚未探索的路径部分的预期代价
```

# 二、代价函数公式

$$
### Algorithm 1 的具体实现：

pseudocode

CalCostHat(x_start, x_goal, x_c, C_sc):
    λ₁ = EuclideanDistance(x_start, x_c)
    μ₁ = EuclideanDistance(x_goal, x_c)
    if λ₁ ≤ 0:
        return ∞  # 避免除零错误
    else:
        return C_sc × (1 + μ₁/λ₁)  # F̂(c) = G(c) × (1 + μ₁/λ₁)
$$
# 三、构建启发式

```c++
### 构建启发式采样范围：

用估计的 F̂(c) 作为超椭球体的**长轴直径**，定义启发式子集：


ζ(x) = {x ∈ X_free: ‖x_start-x‖ + ‖x_goal-x‖ ≤ F̂(c)}
```


# 四、启发式采样范围---用超椭球体定义知情子集

==数学定义==
$$
ζ(x) = {x ∈ X_free | ‖x_start - x‖ + ‖x_goal - x‖ ≤ F̂(c)}
$$
	**含义**：子集`ζ(x)`包含所有 “到起点的欧氏距离 + 到终点的欧氏距离 ≤ F̂(c)” 的无碰撞节点（`x ∈ X_free`）
	**合理性**：根据三角形不等式，“x 到起点距离 + x 到终点距离” 是 “起点→x→终点” 的路径长度下限，若该值≤`F̂(c)`，则 x 有可能构成比 “起点→c→终点” 更优的路径（总长度更短），因此只在`ζ(x)`中采样能提升优化效率。
==F̂(c)是超椭球体的长轴直径，定义启发式子集ζ(x)，目的是控制采样范围==
```c++
其中

**焦点**：超椭球体的两个焦点分别是`x_start`（起点）和`x_goal`（终点）
**长轴直径**：F̂(c)（“起点→c→终点” 的总路径代价估计值），决定了采样区域的范围
**短轴直径**：由公式`√(F̂(c)² - ‖x_start - x_goal‖²)`计算，其中`‖x_start - x_goal‖`是起点到终点的直线距离（焦距），符合超椭球体的几何性质（长轴 ² = 2× 焦距 ² + 短轴 ²）。
```
## 1、超椭球体实现伪代码
```cpp
# 伪代码：椭圆更新逻辑
current_best_cost = ∞

while not reached_goal:
    # 每次找到更好路径时才收缩椭圆
    if new_path_cost < current_best_cost:
        current_best_cost = new_path_cost
        update_ellipse(current_best_cost)  # 收缩椭圆
    else:
        # 保持当前椭圆，继续在现有范围内优化
        continue
```

# 五、具体实现流程
```
阶段1：探索阶段（开始）
  - 椭圆较大，近似整个空间
  - 行为类似传统RRT，广泛探索

阶段2：优化阶段（找到初始路径后）
  - 椭圆开始收缩
  - 采样越来越集中
  - Pareto支配开始选择新的起点

阶段3：收敛阶段
  - 椭圆很小，高度集中
  - 快速优化到最后路径
```
# 六、超椭球体缩减范围----创新点

注：可以给出自适应比例因子[^1]


```python
def adaptive_ellipse_contraction(current_cost, improvement_rate):
    """根据改进速率调整收缩强度"""
    if improvement_rate > 0.1:  # 快速改进阶段
        contraction_factor = 0.7  # 强力收缩
    elif improvement_rate > 0.01:  # 平稳改进阶段  
        contraction_factor = 0.9  # 温和收缩
    else:  # 停滞阶段
        contraction_factor = 1.1  # 反而扩大搜索范围！
    
    return current_cost * contraction_factor
```


# 七、Pareto支配实现
## ## Pareto支配选择新起点的详细实现

### 1. **完整的Pareto选择流程**

#### **步骤1：构建节点评价向量**

对树中每个节点x，构造二维评价向量：

python

vector_x = [-out_degree(x), estimated_cost(x)]

其中：

- `out_degree(x)` = 节点x的子节点数量（反映局部优化潜力）
    
- `estimated_cost(x)` = 从起点经x到终点的估计代价
    
- **负号**：因为Pareto支配默认最小化，但我们希望出度最大化
    

#### **步骤2：建立Pareto支配关系**

python

def dominates(vector_a, vector_b):
    """判断vector_a是否支配vector_b"""
    # 所有维度都不差
    all_not_worse = (vector_a[0] <= vector_b[0] and 
                     vector_a[1] <= vector_b[1])
    # 至少一个维度严格更好
    strictly_better = (vector_a[0] < vector_b[0] or 
                       vector_a[1] < vector_b[1])
    
    return all_not_worse and strictly_better

#### **步骤3：计算支配计数**

python

## 为每个节点初始化支配计数
domination_count = {node: 0 for node in tree_nodes}

## 遍历所有节点对
for i, node_i in enumerate(tree_nodes):
    for j, node_j in enumerate(tree_nodes):
        if i != j and dominates(vector_i, vector_j):
            domination_count[node_i] += 1

#### **步骤4：选择Pareto最优集**

python

def find_pareto_optimal(domination_count):
    # 找到支配计数最大的节点
    max_count = max(domination_count.values())
    pareto_set = [node for node, count in domination_count.items() 
                  if count == max_count]
    return pareto_set

### 2. **具体执行示例**

**场景**：有6个候选节点

text

节点数据：
A: 出度=3, 估计代价=85  → 向量=[-3, 85]
B: 出度=2, 估计代价=90  → 向量=[-2, 90]  
C: 出度=3, 估计代价=60  → 向量=[-3, 60]
D: 出度=1, 估计代价=40  → 向量=[-1, 40]
E: 出度=0, 估计代价=50  → 向量=[0, 50]
F: 出度=0, 估计代价=80  → 向量=[0, 80]

**支配关系分析**：

text

C vs A: [-3,60] vs [-3,85] → C支配A（代价更小）
C vs B: [-3,60] vs [-2,90] → C支配B（两个维度都更好）
C vs D: [-3,60] vs [-1,40] → 无支配关系（各有优劣）
C vs E: [-3,60] vs [0,50]  → 无支配关系
C vs F: [-3,60] vs [0,80]  → C支配F

D vs E: [-1,40] vs [0,50]  → D支配E
D vs F: [-1,40] vs [0,80]  → D支配F

**支配计数结果**：

text

A: 支配0个 → 计数=0
B: 支配0个 → 计数=0  
C: 支配A,B,F → 计数=3
D: 支配E,F → 计数=2
E: 支配0个 → 计数=0
F: 支配0个 → 计数=0

**Pareto最优选择**：节点C（计数最大=3）

### 3. **工程实现优化**

#### **避免O(n²)复杂度**

python

## 使用快速非支配排序（NSGA-II中的方法）
def fast_non_dominated_sort(nodes):
    fronts = [[]]
    for p in nodes:
        p.dominated_set = []
        p.domination_count = 0
        
        for q in nodes:
            if dominates(p.vector, q.vector):
                p.dominated_set.append(q)
            elif dominates(q.vector, p.vector):
                p.domination_count += 1
                
        if p.domination_count == 0:
            fronts[0].append(p)
    
    return fronts

# 八、经验预测器的具体实现方式

### 1. **完整的CalCostHat实现**


```python

def CalCostHat(x_start, x_goal, x_c, actual_cost_to_c):
    """
    基于已探索路径经验，预测总路径代价
    
    参数:
        x_start: 起点坐标 (n维向量)
        x_goal: 终点坐标 (n维向量) 
        x_c: 当前评估节点坐标 (n维向量)
        actual_cost_to_c: 从起点到x_c的实际路径长度
    
    返回:
        估计的总路径代价: 起点 → x_c → 终点
    """
    
    # 步骤1: 计算欧氏距离
    euclidean_start_to_c = np.linalg.norm(x_c - x_start)
    euclidean_c_to_goal = np.linalg.norm(x_goal - x_c)
    
    # 步骤2: 安全检查
    if euclidean_start_to_c <= 1e-10:  # 避免除零
        return float('inf')
    
    # 步骤3: 计算绕行比例因子
    detour_ratio = actual_cost_to_c / euclidean_start_to_c
    
    # 步骤4: 估计后半段路径代价
    estimated_cost_c_to_goal = euclidean_c_to_goal * detour_ratio
    
    # 步骤5: 返回总代价估计
    total_estimated_cost = actual_cost_to_c + estimated_cost_c_to_goal
    
    return total_estimated_cost

```
### 2. **绕行比例因子的物理意义**

**实际计算过程**：
```

text

已知数据：
  起点到c的直线距离: 5.0单位
  起点到c的实际路径: 7.5单位（有绕行）
  c到终点的直线距离: 8.0单位

计算：
  绕行比例 = 7.5 / 5.0 = 1.5
  估计c到终点路径 = 8.0 × 1.5 = 12.0单位
  总估计 = 7.5 + 12.0 = 19.5单位

```
**为什么有效**：

- 如果前半段绕行严重（比例>1），说明该区域障碍密集
    
- 后半段很可能也需要类似程度的绕行
    
- 如果前半段很直接（比例≈1），后半段也可能比较直接
    

### 3. **改进的经验预测器**

#### **考虑环境记忆**：


```python

class ExperiencePredictor:
    def __init__(self):
        self.region_detour_ratios = {}  # 区域绕行经验库
        
    def predict_cost(self, x_start, x_goal, x_c, actual_cost_to_c):
        # 获取当前区域特征
        region_feature = self.get_region_feature(x_start, x_c)
        
        # 如果有历史经验，使用区域平均绕行比例
        if region_feature in self.region_detour_ratios:
            avg_detour = self.region_detour_ratios[region_feature]
        else:
            # 使用当前路径的绕行比例
            avg_detour = actual_cost_to_c / euclidean_distance
        
        # 更新经验库
        self.update_experience(region_feature, avg_detour)
        
        return actual_cost_to_c + euclidean_c_to_goal * avg_detour

```
#### **多维度经验融合**：


```python

def multi_factors_predictor(x_start, x_goal, x_c, actual_cost_to_c, tree):
    # 因素1: 当前路径绕行比例
    basic_ratio = actual_cost_to_c / distance(x_start, x_c)
    
    # 因素2: 局部障碍物密度估计
    local_obstacle_density = estimate_local_density(x_c, tree)
    
    # 因素3: 方向一致性（路径方向与目标方向的夹角）
    direction_alignment = calculate_alignment(x_start, x_c, x_goal)
    
    # 综合预测
    combined_ratio = (basic_ratio * 0.6 + 
                     local_obstacle_density * 0.3 +
                     direction_alignment * 0.1)
    
    return actual_cost_to_c + distance(x_c, x_goal) * combined_ratio
```
# 九、完整防局部最优系统

### **集成解决方案**：

```python

class RobustDynamicRRT:
    def __init__(self):
        self.ellipse_controller = AdaptiveEllipseController()
        self.pareto_selector = ParetoSelector()
        self.experience_predictor = ExperiencePredictor()
        
    def plan_path(self):
        while not self.reached_goal():
            # 1. 自适应椭圆采样
            sample_point = self.ellipse_controller.sample()
            
            # 2. 扩展树
            new_node = self.extend_tree(sample_point)
            
            # 3. 经验预测更新
            estimated_cost = self.experience_predictor.predict(new_node)
            
            # 4. 防局部最优的Pareto选择
            if self.should_update_start():
                new_start = self.pareto_selector.select_with_exploration()
                self.update_start(new_start)
            
            # 5. 智能椭圆更新
            if self.has_significant_improvement():
                self.ellipse_controller.contraction_update()
            elif self.is_stagnant():
                self.ellipse_controller.expansion_update()
```


[^1]: class AdaptiveScalingFactor:
	    def __init__(self):
	        self.history = []  # 存储历史路径的比例关系
	        
	    def update_factor(self, actual_length, mbr_volume, total_volume):
	        """根据新路径更新比例因子估计"""
	        current_alpha = actual_length / (mbr_volume / total_volume)
	        self.history.append(current_alpha)
	        
	        # 使用滑动窗口平均
	        if len(self.history) > 10:
	            self.history.pop(0)
	            
	        return np.mean(self.history)
	    
	    def predict_cost(self, G_c, λ₁, μ₁):
	        """使用自适应比例因子预测代价"""
	        adaptive_ratio = self.current_factor
	        # 更精细的代价预测模型...
