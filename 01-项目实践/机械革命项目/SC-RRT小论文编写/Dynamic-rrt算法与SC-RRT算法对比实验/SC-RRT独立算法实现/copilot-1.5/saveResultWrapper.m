function saveResultWrapper(name, config, dimension, env_idx, path, tree, elapsedTime, obstacles, fig, gif_save_path)
    % 结果保存函数，仅供内部调用
    saveResult(name, config, dimension, env_idx, path, tree, elapsedTime, obstacles, fig, gif_save_path);
end